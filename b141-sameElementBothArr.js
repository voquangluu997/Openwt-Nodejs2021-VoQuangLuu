function sameElementsBothArr(arr1, arr2) {
  let count = 0;
  for (let i = 0; i < arr1.length; i++)
    for (let j = 0; j < arr[2].length; j++) {
      if (arr1[i] == arr2[j]) count++;
    }
  return count;
}
